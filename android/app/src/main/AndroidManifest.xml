<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.Dialog"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Widget setup. Also launchable from the app drawer so the server
             address can be changed later without re-placing the widget. -->
        <activity
            android:name=".ConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Must be exported: the system delivers APPWIDGET_UPDATE to it. -->
        <receiver
            android:name=".UsageWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- Tap-to-refresh. No intent-filter -> exported=false by default, so
             only our own PendingIntent (which names it by class) can reach it,
             not any other app broadcasting the REFRESH action. -->
        <receiver
            android:name=".RefreshReceiver"
            android:exported="false" />

    </application>
</manifest>
